<a id="readme-top"></a>

<!-- ABOUT THE PROJECT -->
## TSS++

On ~~7/10~~ ~~7/20~~ 7/21 8:00 am PST. A new student info system called Triton Student System (TSS) was released. Triton Student System (TSS), replacing the beloved WebReg that carried generations of students through enrollment seasons.

TSS++ Offers an alternative way to browse through the schedule of classes, and plan your schedule (without TritonGPT). It also features a live prerequisite viewer to assist with course planning, as well as exporting built schedules to standard .ics formats.

### Built With

* **Scrapers:** Python 3.11+ (httpx / requests + BeautifulSoup) + MongoDB
* **Backend:** FastAPI, Pydantic, PyMongo, MongoDB
* **Frontend:** React 18, TypeScript, Vite, Tailwind CSS

### Repo Structure

```
tssplusplus/
├── scrapers/                  the scraping pipeline
│   ├── catalog_scraper/       public catalog scraper -> data/catalog/<CODE>.json
│   ├── tss_scraper/           live TSS section/meeting scraper -> data/offered/<term>.csv + MongoDB
│   └── mark_offered_courses.py  cross-references the two, sets offered_this_qtr in data/catalog/
├── data/                      scraper output -- checked in, regenerated by the scrapers
│   ├── catalog/<CODE>.json    per-department course/prereq data
│   └── offered/<term>.csv     per-term list of offered courses
├── backend/                   FastAPI service reading data/ + MongoDB -- see backend/README.md
├── frontend/                  React + TypeScript web app -- see frontend/README.md
└── requirements.txt           
```

### Architecture

```
              +----------------------------------------+
              |             scrapers/ (done)            |
              |  catalog_scraper, tss_scraper,          |
              |  mark_offered_courses.py                |
              +--------------------+---------------------+
                                   | writes
                                   v
              +----------------------------------------+
              |                  data/                  |
              |  data/catalog/*.json (courses, prereqs, |
              |    offered_this_qtr)                    |
              |  MongoDB (sections, meetings, seat/     |
              |    waitlist counts)                     |
              +--------------------+---------------------+
                                   | reads
                                   v
              +----------------------------------------+
              |                backend/                 |
              |  FastAPI service                        |
              |  - GET /api/courses (search/filter)     |
              |  - GET /api/courses/{module_id}         |
              |    (detail: catalog + live sections)    |
              |  - GET /api/courses/{module_id}/prereqs |
              |    (transitive prereq graph, Mongo-cached)|
              +--------------------+---------------------+
                                   | HTTP/JSON API
                                   v
              +----------------------------------------+
              |                frontend/                 |
              |  Vite + React + TypeScript              |
              |  - course search & catalog view         |
              |  - prerequisite tree display             |
              |  - schedule planner                      |
              |  (currently wired to mock data, not the |
              |   backend API yet -- see frontend/README.md)|
              +--------------------+---------------------+
                                   |
                                   v
                                 user
```

`scrapers/`, `data/`, `backend/`, and `frontend/` all exist today.
The backend serves the three endpoints above; the frontend is
scaffolded with mock data and isn't wired to them yet.

## Getting Started

To get a local copy up and running follow these steps.

### Prerequisites

* Python 3.11+ and pip
* Node.js + npm -- for the frontend
* A MongoDB instance (local or Atlas) -- needed for `tss_scraper`'s section/meeting data and the backend API

### Installation

1. Clone the repo
   ```sh
   git clone https://github.com/github_username/repo_name.git
   ```
2. Install Python dependencies
   ```sh
   pip install -r requirements.txt
   ```
3. Add `MONGODB_URI` (and optionally `MONGODB_DB`) to a `.env` file at the repo root -- used by `tss_scraper` and the backend. For `tss_scraper` you'll also need a TSS login cookie -- see [`scrapers/tss_scraper/README.md`](scrapers/tss_scraper/README.md) for the full walkthrough
4. Run the scrapers -- see [`scrapers/catalog_scraper/README.md`](scrapers/catalog_scraper/README.md) and [`scrapers/tss_scraper/README.md`](scrapers/tss_scraper/README.md)
5. Run the backend -- see [`backend/README.md`](backend/README.md)
   ```sh
   cd backend
   uvicorn app.main:app --reload
   ```
6. Run the frontend -- see [`frontend/README.md`](frontend/README.md)
   ```sh
   cd frontend
   npm install
   npm run dev
   ```





## Roadmap

- [x] Static catalog scraper
- [x] Dynamic TSS Scraper
- [x] MongoDB setup
- [x] Initial UI/UX Design
- [x] Set up api Endpoints
- [ ] Connect frontend + backend
- [ ] Class Search Feature
- [ ] Course Planner Feature
- [ ] Prerequisite Viewer Feature
- [ ] ICS Export Feature





<!-- CONTRIBUTING -->
## Contributing

If you would like to contribute, send me a message on discord @alowo.


<!-- LICENSE -->

## Acknowledgments


* [WebReg++](https://github.com/yxli001/webregplusplus): An archived site i used when i first enrolled, featuring an easier way of browsing offered courses
* [ClassGraph](https://github.com/nehalc200/classgraph): Prerequisite viewer i built under a team as apart of DS3's WI26 Projects cohort. Found it very useful
* Shoutout Claude Code

